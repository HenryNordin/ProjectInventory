<Window x:Class="ProjectInventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectInventory"
        mc:Ignorable="d"
        Title="Inventory Manager" Height="720" Width="640"
        WindowStartupLocation="CenterScreen"
        Background="#FF292929">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Item Manager"
                   Grid.Row="0"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0 0 0 20"
                   Foreground="White"/>

        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            Margin="0 0 0 20">

            <TextBox x:Name="txtName"
             Width="100"
             Height="35"
             VerticalContentAlignment="Center"
             Margin="0 0 10 0"
             Padding="5"
             Text="Item Name"/>

            <TextBox x:Name="txtDescription"
             Width="300"
             Height="35"
             VerticalContentAlignment="Center"
             Margin="0 0 10 0"
             Padding="5"
             Text="Description"/>

            <Button x:Name="btnAdd"
            Content="Add"
            Width="80"
            Height="35"
            Margin="0 0 10 0"
            Click="btnAdd_Click"/>

            <Button x:Name="btnDelete"
            Content="Delete"
            Width="80"
            Height="35"
            Click="btnDelete_Click"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5"
                    Background="#FF1E1E1E"
                    Margin="0 0 10 0">
                <ListBox x:Name="lstItems"
                         d:ItemsSource="{d:SampleData ItemCount=10}"
                         Foreground="White"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="10"
                         FontSize="14" SelectionChanged="lstItems_SelectionChanged"/>
            </Border>

            <TextBox x:Name="TxtBoxInfo"
                     Grid.Column="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="10"
                     Foreground="White"
                     Background="#FF1E1E1E"
                     BorderBrush="Gray"
                     BorderThickness="1"/>
        </Grid>
    </Grid>
</Window>
